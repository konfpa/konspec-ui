## What this changes

<!-- One line. If it adds a component or variant, link the issue that requested it. -->

## Checklist

- [ ] `assets/spec.js` / `assets/reg/*.js` are the only files I edited by hand
- [ ] `node tools/build.js` run, and its output committed in the same commit
- [ ] `node tools/build.js --check` passes
- [ ] Checked at 390px — nothing scrolls sideways
- [ ] Operable by keyboard, and focus is visible everywhere it lands
- [ ] Status colour used only for what a record is *doing*, never for decoration
